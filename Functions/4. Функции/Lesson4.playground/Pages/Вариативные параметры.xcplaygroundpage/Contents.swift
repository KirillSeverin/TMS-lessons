//: [Previous](@previous)

import Foundation

//: ### Вариативные параметры - могут иметь несколько значений или ни одного
// Посчитаем среднее арифметическое
// Входящим может быть только 1 параметр и сколько угодно значений

/*
func arithmeticMean(_ numbers: Double...) -> Double {
    var total = 0.0
    for number in numbers { // перебираем массив в цикле
        total += number
    }
    return round(total / Double(numbers.count) * 1000) / 1000
}

arithmeticMean(4, 4, 5, 3, 4, 5) // далее перобразование в массив и выполнение
*/
//: [Next](@next)
